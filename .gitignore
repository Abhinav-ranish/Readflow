node_modules
dist
.DS_Store
.env
.env*.local

# Promo - ignore output video and build artifacts
promo/out
promo/node_modules
promo/dist

# Claude / swarm runtime data
/.claude-flow
/.claude
/readex/.claude-flow
/readex/.data
/readex/.readflow
.swarm/state.json

# Can Uncommit later (rn only local files, may contain sensitive info)
/docs
.mcp.json
CLAUDE.md
/.agents
/.readflow
